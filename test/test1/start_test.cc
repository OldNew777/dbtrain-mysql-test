#include "backend/backend.h"
#include "gtest/gtest.h"

namespace dbtrain_mysql {

TEST(StartTest, EmptyTest) {
  // test whether googletest works
  int a = 1;
  EXPECT_EQ(a, 1);
}

}  // namespace dbtrain_mysql
